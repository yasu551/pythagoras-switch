import { CourseBuilder } from '../CourseBuilder';

// Funnel, flag, and finale animation are handled by Stage6_Elevator's scripted continuation.
export function buildStage8(_cb: CourseBuilder): void {
  // No-op: all visuals built in Stage 6, all animation in performScriptedContinuation
}
