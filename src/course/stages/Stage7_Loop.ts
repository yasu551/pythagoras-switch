import { CourseBuilder } from '../CourseBuilder';

// Loop visuals and animation are handled by Stage6_Elevator's scripted continuation.
export function buildStage7(_cb: CourseBuilder): void {
  // No-op: all visuals built in Stage 6, all animation in performScriptedContinuation
}
